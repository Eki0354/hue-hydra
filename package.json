{
  "name": "hue-hydra",
  "version": "1.0.9",
  "description": "A TypeScript-based npm package for generating SVG code with special filters, enabling you to completely replace a specified color in an image with another RGBA color",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "patch": "npm version patch",
    "pub": "npm publish --access public && git push && git push --tags",
    "lint": "eslint src --ext .ts",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Eki0354/hue-hydra.git"
  },
  "keywords": [
    "Hue Hydra",
    "svg",
    "filter",
    "Color Keying"
  ],
  "author": "Eki <ichinoseeki@outlook.com>",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0"
  }
}
